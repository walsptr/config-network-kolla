---
- name: Render managed netplan configuration
  ansible.builtin.template:
    src: netplan.yaml.j2
    dest: "{{ network_netplan_file }}"
    owner: root
    group: root
    mode: '0600'
  register: network_netplan_template_result

